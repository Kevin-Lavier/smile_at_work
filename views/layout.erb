<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Smile at Work</title>
  <!-- Favicon: displayed in the browser tab -->
  <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
  <!-- Custom stylesheet -->
  <link rel="stylesheet" href="/styles/style.css">
  <!-- Google Font: Boldonse -->
  <link href="https://fonts.googleapis.com/css2?family=Boldonse&display=swap" rel="stylesheet">
  <!-- Tailwind CSS via CDN -->
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="<%= @body_class || 'bg-gray-100 text-gray-900 flex flex-col min-h-screen' %>">
  <!-- The body class is dynamic: it uses @body_class if set, otherwise a default style is applied -->

  <!-- Fixed Header at the top -->
  <header class="fixed top-0 left-0 right-0 p-4 z-50">
    <div class="container mx-auto flex items-center justify-between">
      <!-- Logo: links to the homepage -->
      <div class="logo">
        <a href="/"><img src="/images/logo_smile_at_work.png" alt="Logo de Smile At Work" class="max-w-[100px]"></a>
      </div>
      <!-- Navigation links: conditionally rendered based on current_user -->
      <div class="flex items-center space-x-4">
        <% if current_user %>
          <%# When a user is logged in, show Profile, Mood, and Logout links %>
          <a href="/profile" class="px-3 py-1 bg-white/50 border text-black font-semibold rounded hover:bg-gray-200/50">
            Profil
          </a>
          <a href="/mood" class="px-3 py-1 bg-white/50 border text-black font-semibold rounded hover:bg-gray-200/50">
            Humeur
          </a>
          <a href="/logout" class="px-3 py-1 bg-white/50 border text-black font-semibold rounded hover:bg-gray-200/50">
            Déconnexion
          </a>
        <% else %>
          <%# When no user is logged in, show only the Login link %>
          <a href="/login" class="px-3 py-1 bg-white/50 border text-black font-semibold rounded hover:bg-gray-200/50">
            Connexion
          </a>
        <% end %>
      </div>
    </div>
  </header>

  <!-- Main content: includes padding to avoid being hidden behind the fixed header and footer -->
  <main class="pt-24 pb-20 container mx-auto px-4">
    <%= yield %>
  </main>

  <!-- Fixed Footer at the bottom -->
  <footer class="fixed bottom-0 left-0 right-0 bg-gray-800/50 text-gray-300 text-center py-3 text-sm z-50">
    &copy; <%= Time.now.year %> Colint School. Tous droits réservés.
  </footer>
</body>
</html>
